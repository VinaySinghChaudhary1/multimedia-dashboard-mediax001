document.addEventListener('DOMContentLoaded', function() { fetch('sales.xlsx').then(response => response.arrayBuffer()).then(data => { const workbook = XLSX.read(data, { type: 'array' }); const firstSheetName = workbook.SheetNames[0]; const worksheet = workbook.Sheets[firstSheetName]; const html = XLSX.utils.sheet_to_html(worksheet, { id: 'excel-table', editable: true }); document.getElementById('excel-table').innerHTML = html; }).catch(error => { const alertDiv = document.createElement('div'); alertDiv.className = 'alert alert-danger'; alertDiv.textContent = 'Error loading Excel data.'; document.getElementById('excel-table').appendChild(alertDiv); }); });